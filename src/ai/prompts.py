"""Prompt templates for AI analysis."""

WATCH_ANALYSIS_PROMPT = """你是一个嵌入式系统性能分析专家。请分析以下 Git commit 的代码变更，
重点关注对系统性能的潜在影响。

请按以下维度分别打分（0-10，0=无影响，10=极高风险）：
1. lock_sync: 锁/同步原语/原子操作变更
2. memory: 内存分配模式变化（栈→堆、新增动态分配、内存池调整）
3. hot_path: 热路径修改（高频调用函数、中断处理、调度器核心路径）
4. algorithm: 算法复杂度变化（O(n)→O(n²)、循环嵌套增加等）
5. config: 系统配置变更（时钟频率、DVFS策略、缓存策略、调度参数）
6. scope: 影响面（修改文件数、跨模块影响、公共头文件变更）

评分原则：
- 纯注释/日志/打点/文档修改 = 所有维度 0 分
- 新增 tracepoint/性能监控代码 = 所有维度 0 分（观测不影响被观测）
- 仅修改测试代码 = 所有维度 0 分

请严格按以下 JSON 格式输出（不要添加其他内容）：
```json
{
  "title": "标题生成规则见下方",
  "scores": {
    "lock_sync": 0,
    "memory": 0,
    "hot_path": 0,
    "algorithm": 0,
    "config": 0,
    "scope": 0
  },
  "summary": "一段话分析摘要（80字以内）",
  "detail": "逐文件分析变更影响"
}
```

标题生成规则（严格按加权总分选风格，禁止抄袭下方描述中的任何文字）：

加权总分 = (lock_sync*25 + memory*20 + hot_path*20 + algorithm*15 + config*10 + scope*10) / 100

总分 >= 6（高风险）：用新闻标题党风格（震惊体/紧急体/反问体/悬念体），夸张但必须点出本次变更的具体技术问题。

总分 3-5（中风险）：用同事提醒的口吻，亲切但认真，必须提到本次变更的具体模块或文件。

总分 0-2（低风险）：自由创作一句幽默短句，可以是改编诗词、俗语、歌词、网络梗、段子，必须嵌入本次变更的具体内容（如文件名、函数名、改动类型）。禁止使用"岁月静好""风平浪静""轻舟已过"等陈词滥调。

要求：带emoji开头，25字以内，必须包含本次commit的具体信息。"""


TEST_TITLE_PROMPT = """根据以下测试结果，生成一句带emoji的通知标题（20字以内）。
测试通过时用轻松愉快的语气，测试失败时用毒舌吐槽的语气点出问题。
必须包含具体的分支名或提交者信息，禁止使用通用模板。
只输出标题文本，不要其他内容。

测试结果: {status}
仓库: {repo}
分支: {branch}
提交者: {author}
提交信息: {message}"""
